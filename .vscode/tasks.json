{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "linux-dev-build",
            "type": "shell",
            "command": "${workspaceRoot}/builder/build",
            "args": [
                "dev-linux"
            ],
            "problemMatcher": []
        },
        {
            "label": "windows-dev-build",
            "type": "shell",
            "command": "${workspaceRoot}/builder/build",
            "args": [
                "dev-windows"
            ]
        },
        {
            "label": "mac-dev-build",
            "type": "shell",
            "command": "${workspaceRoot}/builder/build",
            "args": [
                "dev-mac"
            ]
        }
    ]
}

